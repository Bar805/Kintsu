First check whether a new environment variable is needed to use the suggested place function. Then address this issue:

- When the AI suggested a place to meet, the AI doesn't seem to consider the context properly. For example, in a chat session where the two users already mentioned an actual place name, the AI still suggested somewhere else.
