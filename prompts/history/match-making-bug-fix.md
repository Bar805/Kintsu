There is an intermittent error where user might submit a match request and forever stays in the "matching phase". The only way to fix it is to delete that match request record in supabase. Try to find the error's reason. Then make sure you implement a failure management logic such that if there's an error happening, user falls back to the previous state and can resubmit another request
