The current project contains problem when the Gemini model reports a 503 service unavailable error. I think to solve this, we should try other models when an error has happened. Make sure this error handling is applied to all API calls so some abstraction is needed to modularize the code. The other models to try on should be gemini-2.5-pro, gemini-3-flash-preview, gemini-3.1-pro-preview in this exact order.
